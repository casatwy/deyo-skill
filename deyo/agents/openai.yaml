interface:
  display_name: "Deyo"
  short_description: "使用已安装的 deyo 命令发起链接或本地音视频文件转写，并在执行过程中向用户同步任务进度"
  default_prompt: "Use $deyo to install, configure, or run the published deyo transcription CLI for links or local audio/video files, and keep the user updated on upload, media-check, task, and transcription progress."

policy:
  allow_implicit_invocation: true
